# project1ML
Rules: PyTorch only. Must use my code and modify from there.  Train and test a pytorch model from scratch using the CIFAR 10 code. You can incorporate a pre-trained model from torchvision into the code as long as you fine tune it with the cifar data. The pre-trained model cannot have been pre-trained on CIFAR 10 data. 
